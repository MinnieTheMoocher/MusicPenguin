{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Launch Electron",
      "runtimeExecutable": "npm",
      "runtimeArgs": ["run", "electron"],
      "preLaunchTask": "build",
      "console": "integratedTerminal",
      "cwd": "${workspaceFolder}"
    }
  ]
}
